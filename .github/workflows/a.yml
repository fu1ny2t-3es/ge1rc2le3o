on:
  workflow_dispatch:
  schedule:
    - cron: '45 */168 * * *'


jobs:

  cron:
    runs-on: ubuntu-latest

    steps:
      - uses: fu1ny2t-3es/ge1rc2le3o@cron
        with:
          token: ${{ secrets.token }}
